// Mock modules for TanStack Start virtual imports
// These are only needed for bundling; not used at runtime in Cloudflare Workers context

export const tsrStartManifest = {};
export const injectedHeadScripts = [];
export default {};
